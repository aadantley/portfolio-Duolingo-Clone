import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  root: {
    flexDirection: "row",
    padding: 10,
    alignItems: "center",
  },
  icon: {
    height: 30,
    width: 30,
    marginHorizontal: 10,
  },
  lives: {
    color: "red",
    fontWeight: "bold",
    fontSize: 18,
  },
  xIcon: {
    height: 40,
    width: 20,
    backgroundColor: "red",
  },
});

export default styles;
